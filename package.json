{
  "name": "opencode-better-share",
  "private": true,
  "workspaces": [
    "web",
    "plugin"
  ],
  "packageManager": "bun@1.3.5",
  "scripts": {
    "dev": "bun run --filter web dev",
    "build": "bun run --filter '*' build",
    "format": "biome format --write .",
    "lint": "biome lint .",
    "check": "biome check --write ."
  },
  "devDependencies": {
    "@biomejs/biome": "^2.3.11",
    "typescript": "^5.7.2"
  }
}
